= form_for @event do |f|
  = render 'shared/error_messages', :object => f.object  
  = f.label :naam
  = f.text_field :name
  = f.label :geslacht
  = collection_select(:event, :sex_id, Sex.all, :id, :name)
  = f.submit
	